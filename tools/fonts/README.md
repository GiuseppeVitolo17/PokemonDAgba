# Fonts folder

The project uses a **hand-edited font** that already includes Danish characters (Æ, Ø, Å, æ, ø, å). Copy the font image from Desktop to `graphics/fonts/latin_normal.png` (and latin_male/female), or use `scripts/import_font_from_desktop.py` from the repo root. See `DANISH_FONT.md`.
